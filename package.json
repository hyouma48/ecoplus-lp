{
  "name": "ecoplus-lp",
  "version": "0.1.0",
  "private": true,
  "description": "Google Ads landing page for Eco Plus+ used-item pickup buyback service.",
  "scripts": {
    "serve": "http-server -p 8080 -c-1 --silent",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.0",
    "http-server": "^14.1.1"
  }
}
